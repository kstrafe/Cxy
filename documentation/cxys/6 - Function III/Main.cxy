(64u choose : 32u any, 32u name, 32u here : pure) sum
{
	return choose : any + name + here;
}

(float out : float in) sin
{
	var float x = in;
	var float y = x - x*x*x/6 + x*x*x*x*x/120 - x*x*x*x*x*x*x/5040;
	return y;
}

(:) enter
{
	sml.In.read().toFloat();
}
